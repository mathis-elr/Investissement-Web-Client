@using Investissement_WebClient.Application.DTO

@foreach (var actif in InfoInvestParActif)
{
   <_InfosActif Actif="actif"/>
}

@code {
    [Parameter] public IEnumerable<InfoValeurParActifDto> InfoInvestParActif { get; set; } = [];
}
