@foreach (var actif in InfoInvestParActif)
{
    <_CompteTradeRepubliqueInfosActif Actif="actif" />
}

@code {
    [Parameter] public IEnumerable<InfoParActifDto> InfoInvestParActif { get; set; } = [];
}
